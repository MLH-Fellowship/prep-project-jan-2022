/**
 * @module GeoLocation
 */

/**
 * Geolocation (lat, long) for a location.
 * @class
 * @private
 * @constructor
 */
export default class Geolocation {
  constructor(lat, long) {
    this.lat = lat;
    this.lon = long;
  }
}
